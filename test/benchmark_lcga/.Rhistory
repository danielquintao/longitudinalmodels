setwd("C:/Users/danie/Desktop/3A/Stage3A/longitudinalmodels/test/benchmark_lcga")
library(flexmix)
dataset <- read.csv("benchmark6_to_df.csv", header=TRUE)
m = stepFlexmix(observation ~ time|individual, data=dataset, k = 2, nrep=10,
control=list(verbose=1,classify="weighted"))
m = stepFlexmix(observation ~ time|individual, data=dataset, k = 2, nrep=10,
control=list(classify="weighted"))
parameters(m)
parameters(m)["sigma",]^2
prior(m)
dataset <- read.csv("benchmark7_to_df.csv", header=TRUE)
m = stepFlexmix(observation ~ time|individual, data=dataset, k = 3, nrep=10,
control=list(classify="weighted"))
parameters(m)
parameters(m)["sigma",]^2
prior(m)
library(flexmix)
dataset <- read.csv("benchmark5_to_df.csv", header=TRUE)
m = stepFlexmix(observation ~ time|individual, data=dataset, k = 2, nrep=10,
control=list(classify="weighted"))
parameters(m)
parameters(m)["sigma",]^2
prior(m)
library(flexmix)
dataset <- read.csv("benchmark7_to_df.csv", header=TRUE)
m = stepFlexmix(observation ~ time|individual, data=dataset, k = 4, nrep=10,
control=list(classify="weighted", iter.max=500))
parameters(m)
parameters(m)["sigma",]^2
prior(m)
dataset <- read.csv("benchmark8_to_df.csv", header=TRUE)
m = stepFlexmix(observation ~ time|individual, data=dataset, k = 4, nrep=10,
control=list(classify="weighted", iter.max=500))
parameters(m)
parameters(m)["sigma",]^2
prior(m)
library(flexmix)
dataset <- read.csv("benchmark9_to_df.csv", header=TRUE)
m = stepFlexmix(observation ~ (time+I(time^2)|individual), data=dataset, k = 3, nrep=10,
control=list(classify="weighted", iter.max=500))
parameters(m)
parameters(m)["sigma",]^2
lmer(observation ~ (time+I(time^2)|individual), data=dataset)
install.packages(lme4)
install.packages('lme4')
lmer(observation ~ (time+I(time^2)|individual), data=dataset)
library(lme4)
lmer(observation ~ (time+I(time^2)|individual), data=dataset)
lmer(observation ~ time|individual+I(time^2|individual), data=dataset)
lmer(observation ~ (time|individual)+I(time^2|individual), data=dataset)
lmer(observation ~ time|individual+I(time^2|individual), data=dataset)
lmer(observation ~ time|individual+I(time^2|individual), data=dataset)
lmer(observation ~ (time|individual)+I(time^2|individual), data=dataset)
lmer(observation ~ (time+I(time^2)|individual), data=dataset)
dataset <- read.csv("benchmark9_to_df.csv", header=TRUE)
m = stepFlexmix(observation ~ (time+I(time^2)|individual), data=dataset, k = 3, nrep=10,
control=list(classify="weighted", iter.max=500))
parameters(m)
parameters(m)["sigma",]^2
prior(m)
dataset <- read.csv("benchmark5_to_df.csv", header=TRUE)
m = stepFlexmix(observation ~ time|individual, data=dataset, k = 2, nrep=10,
control=list(classify="weighted"))
parameters(m)
parameters(m)["sigma",]^2
dataset$time
m = stepFlexmix(observation ~ poly(time|individual, degree=1, raw=TRUE), data=dataset, k = 2, nrep=10,
control=list(classify="weighted"))
parameters(m)
parameters(m)["sigma",]^2
m = stepFlexmix(observation ~ (poly(time, degree=1, raw=TRUE)|individual), data=dataset, k = 2, nrep=10,
control=list(classify="weighted"))
parameters(m)
parameters(m)["sigma",]^2
dataset <- read.csv("benchmark5_to_df.csv", header=TRUE)
m = stepFlexmix(observation ~ time|individual, data=dataset, k = 2, nrep=10,
control=list(classify="weighted"))
parameters(m)
parameters(m)["sigma",]^2
m = stepFlexmix(observation ~ time|individual, data=dataset, k = 2, nrep=10,
)#control=list(classify="weighted"))
parameters(m)
parameters(m)["sigma",]^2
m = stepFlexmix(observation ~ time|individual, data=dataset, k = 2, nrep=10,
control=list(classify="hard"))
parameters(m)
parameters(m)["sigma",]^2
m = stepFlexmix(observation ~ time|individual, data=dataset, k = 2, nrep=10,
control=list(classify="random"))
parameters(m)
parameters(m)["sigma",]^2
weighted
weighted
weighted
m = stepFlexmix(observation ~ time|individual, data=dataset, k = 2, nrep=10,
control=list(classify="weighted"))
parameters(m)
parameters(m)["sigma",]^2
glm(observation ~ time|individual, data=dataset)
glm(observation ~ time, data=dataset)
lme(observation ~ time, data=dataset)
lmer(observation ~ time, data=dataset)
mle(observation ~ time, data=dataset)
install.packages('stats4')
install.packages("stats4")
install.packages("stats4")
install.packages("stats4")
install.packages("stats4")
install.packages("stats4")
library(stats4)
mle(observation ~ time, data=dataset)
obj = function(intercept, slope, sigma) {}
obj = function(intercept, slope, sigma) {
pred = intercept + slope*dataset$time
-sum(dnorm(dataset$observation,mean=pred,sd=sigma,log=TRUE))}
mle(obj)
mle(obj)
mle(0,0,1)
formals(obj)
dataset
obj = function(intercept, slope, sigma) {
pred = intercept + slope*dataset$time
-sum(dnorm(dataset$observation,mean=pred,sd=sigma,log=TRUE))}
obj(1,2,3)
mle(obj, c(1,2,3))
mle(obj, c(1,2,0.5))
pred = intercept + slope*dataset$time
pred = 0 + 1*dataset$time
pred
dnorm(dataset$observation,mean=pred,sd=0.5,log=TRUE))
dnorm(dataset$observation,mean=pred,sd=0.5,log=TRUE)
-sum(dnorm(dataset$observation,mean=pred,sd=0.5,log=TRUE))
mle(obj, c(0,0,0.5))
mle(obj, c(0,0,0.5))
mle(obj, c(1,2,0.5))
dataset <- read.csv("benchmark5_to_df.csv", header=TRUE)
m = stepFlexmix(observation ~ time|individual, data=dataset, k = 1, nrep=10,
control=list(classify="weighted"))
library(flexmix)
dataset <- read.csv("benchmark5_to_df.csv", header=TRUE)
m = stepFlexmix(observation ~ time|individual, data=dataset, k = 1, nrep=10,
control=list(classify="weighted"))
parameters(m)
parameters(m)["sigma",]^2
